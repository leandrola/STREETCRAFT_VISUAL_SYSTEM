# Compact Generation Contract
Required fields: source_identity, mode, profile, camera, preserve, transform, remove, infer, unknown, forbid, semantic_text_lock, material_intensity_delta, occlusion_locks. SAR2 and Archive-aware runtime may enrich the contract but cannot weaken hard locks.
