# Model Adapter Layer

SVS → SAEP → Transformation Plan / Revision Contract → Model Adapter → Image model

Core SVS never stores vendor-specific generation syntax.

Adapters should declare capabilities and locality reliability. A clear Streetcraft diagnosis that cannot be executed reliably by the current model should produce MODEL_SWITCH_ADVISED rather than changing Streetcraft rules.
